name: school_project
description: A new Flutter project.

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.2
  syncfusion_flutter_calendar: ^19.3.43+1
  stacked: ^2.2.5
  cloud_firestore: ^2.5.3
  firebase_core: ^1.7.0
  syncfusion_localizations: ^19.3.45

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
